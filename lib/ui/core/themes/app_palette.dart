import 'dart:ui';

class AppPallete {
  // Application color identity
  static const Color blackPrimary = Color(0xFF101010);
  static const Color yellowSecondary = Color(0xFFFFC200);

  static const Color greyText = Color(0xFF707071);

  // Base color
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
}
